<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ honors edge-to-edge contrast attributes set in the
         base theme; this override exists so Material 3 Theme is picked up
         cleanly on S+ devices. -->
    <style name="Theme.ALUHub" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/navy_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
